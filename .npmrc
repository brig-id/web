engine-strict=true

# Web Awesome Pro NPM registry
@web.awesome.me:registry=https://npm.cloudsmith.io/fortawesome/webawesome-pro/
# (auth token lives in user-level ~/.npmrc, not here — pnpm refuses to expand
# env vars from a committed project .npmrc for exactly this reason)